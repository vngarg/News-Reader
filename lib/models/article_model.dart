class ArticleModel {
  String author, title, description, url, urlToImage, content;

  ArticleModel(
      {this.title,
      this.description,
      this.url,
      this.author,
      this.content,
      this.urlToImage});
}
